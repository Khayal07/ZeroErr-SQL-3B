FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml README.md ./
COPY src ./src
COPY eval ./eval

RUN pip install --no-cache-dir -e . "uvicorn[standard]"

EXPOSE 8000

CMD ["uvicorn", "zeroerr.api.main:app", "--host", "0.0.0.0", "--port", "8000"]